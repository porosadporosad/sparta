// function add (매개변수) {
//     // 함수 내부에서 실행할 로직
// }

function add(x,y) {
    return x + y;
}

let add2 = function(x, y) {
    return x + y;
}

console.log(add(2, 3));

let functionResult = add(3,4);
consolg.log(functionResult);

// input : 함수의 input -> 매개변수(매개체가 되는 변수!)
// output : return문 뒤에 오는 값 : 반환값